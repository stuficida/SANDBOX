let Utils = {
    degree(num) {
        if(num > 0) return num % 360;
        else return 360 + num % 360;
    },
    
    degree2(num) {
        if(num > 0) return num % 360;
        else return num % 360;
    },
}